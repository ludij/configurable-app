{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "ConfigHandler executor",
  "description": "Takes a config file and generates component and element aliasses",
  "type": "object",
  "properties": {
    "config": {
      "type": "string",
      "description": "The name of the config folder in libs/configs/"
    }
  },
  "required": ["config"]
}
